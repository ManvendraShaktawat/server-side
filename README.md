<!--
    Node.js runs on PORT 3000, hence we have a 'proxy' field in package.json
    Clent app runs on PORT 3001

    This CRA project contains code examples of the below:
        1. Express server listening to client's HTTP requests
        2. Dev server is WebpackDevServer, not Express (for HMR)
        3. Authentication routes (login, logout)
        4. Express session and session management route (check-session)
        5. authMiddleware for protected/authenticated routes
        6. Product routes (authenticated)
        7. errorHandler middleware
        8. client-side code to test express routes
 -->
